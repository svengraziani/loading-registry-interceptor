import { UrlFragmentIdGenerator } from './url-fragment-id-generator';

describe('UrlFragmentIdGenerator', () => {
  it('should create an instance', () => {
    expect(new UrlFragmentIdGenerator()).toBeTruthy();
  });
});
